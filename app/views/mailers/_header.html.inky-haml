%spacer{size: '16'}

- if sending_away
  %row
    %columns{large: '12'}
      %center
        %strong
          #{referrer.name} (#{mail_to sender.email})
          = t 'mailer.header.sender_info'

  %hr
