= simple_form_for(@property_note, remote: true) do |f|
  = f.input :property_id, as: :hidden
  = f.input :text, label: "Property Notes:"
  .pull-right
    = f.button :submit, t('property_notes.shared.save'), class: "btn-primary btn-xs"
    %small
      \|
      = link_to t('.property_notes.shared.cancel'), "#", class: "cancel-notes"
