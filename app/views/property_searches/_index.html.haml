- property_search.results.in_groups_of(3, false) do |group|
  .row
    - group.each do |property|
      = render partial: 'property_searches/property', object: property

= render partial: "property_searches/more_link", locals: {property_search: property_search}
