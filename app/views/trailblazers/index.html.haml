.container
  %h2 Trailblazers
  - @users.in_groups_of(3, false) do |group|
    .row
      - group.each do |user|
        = render partial: "profiles/profile", locals: {profile: user.profile, profile_link: author_articles_path(user)}
