.container
  #properties

    #property-list
      - @items.in_groups_of(3, false) do |group|
        .row
          - group.each do |item|
            = render partial: "#{@item_type}_searches/property", object: item
