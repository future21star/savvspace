%h1
  Welcome
  = current_user.email

.list-group
  = link_to(areas_path, class: "list-group-item") do
    %span.badge= Area.count
    Areas
  = link_to(users_path, class: "list-group-item") do
    %span.badge= User.count
    Users
  = link_to(vendors_path, class: "list-group-item") do
    %span.badge= Vendor.count
    Vendors
