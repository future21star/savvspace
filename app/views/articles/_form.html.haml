= simple_form_for(@article) do |f|
  = f.error_notification
  = f.association :area, collection: area_options, label_method: :name, value_method: :id
  = f.input :title
  = f.input :body, as: :wysihtml5
  = f.input :feature_photo, as: :attachinary
  = f.button :submit
